[project]
name = "beachhouse"
version = "0.1.0"
description = "BeachHouse — catálogo semántico y modelado por código"
readme = false
requires-python = ">=3.11"
dependencies = [
  "manifold3d>=3.5.1",
  "scipy>=1.17.1",
  "trimesh>=4.0",
]

[tool.uv]
package = false
